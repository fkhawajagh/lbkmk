# .eap.toml — external-agent-protocol configuration for lbkmk.
# Generated by ~/.dotfiles/skills/external-agent-protocol/scripts/install.
# Edit by hand to tune; re-run install to regenerate from prompts.

project_name = "lbkmk"
root_doc = "CLAUDE.md"
status_path = "docs/.context/{branch}/final-status.md"
read_context_md = false
build_pipeline = ["scripts/build-docs.sh"]

# Optional follow-up tracking (see rules/followup-tracking.md). Uncomment to enable.
# followup_labels    = ["deferred-from-eap"]
# followup_project   = ""
# followup_milestone = ""
# followup_assignee  = ""
# followup_repo      = ""

# Optional sweep-stale search paths (defaults to lib/ test/ docs/ priv/ if unset).
# sweep_paths = ["lib", "test", "docs"]
