# Build artifacts
docs/dist/
docs/_render/
mermaid-filter.err

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Node (for export tooling)
node_modules/
package-lock.json

# Phoenix (when we get there)
_build/
deps/
.elixir_ls/
.fetch
erl_crash.dump
*.beam
priv/static/assets/
priv/static/cache_manifest.json

# Env
.env
.env.local

# Worktrees and per-feature agent context
.worktrees/
docs/.context/

# Session-end handoff bridges (gitignored; only the convention README is committed)
docs/.handoff/*
!docs/.handoff/README.md

/.quarto/
**/*.quarto_ipynb
